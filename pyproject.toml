[project]
name = "evermemos-openapi-samples"
version = "0.1.0"
description = "EverMemOS OpenAPI SDK examples"
requires-python = ">= 3.10"
dependencies = [
    "evermemos==0.3.12",
]

[tool.uv]
managed = true
